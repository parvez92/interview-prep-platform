spring:
  config:
    import: ""   # disable vault import in tests
  cloud:
    vault:
      enabled: false

app:
  jwt:
    secret: test-jwt-secret-that-is-long-enough-for-hs256
    access-token-expiry-ms: 900000
    refresh-token-expiry-ms: 604800000
  cors:
    allowed-origins: http://localhost:5173
  ai-service:
    url: http://localhost:${test.wiremock.port:18080}
    service-token: test-inter-service-token
  storage:
    local-root: ${java.io.tmpdir}/preploop-test-files
