package com.interview.prep.platform.backend_core.config;

import org.springframework.context.annotation.Configuration;

/**
 * Vault property source is bootstrapped via spring.config.import in application.yml.
 * Add VaultTemplate or other Vault beans here as needed in later slices.
 */
@Configuration
public class VaultConfig {
}
